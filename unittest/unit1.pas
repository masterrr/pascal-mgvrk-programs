unit mylib; {unit name = file name!}
{секция связи}
interface
{Константы, типы переменные, заголовки процедур и функций, которые будут доступны из внешнего модуля}
type
    tmas2=array[1..10, 1..10] of real;
var
   i: integer;
{секция реализации}
implementation
{Можно объявлять что хочешь, только локально}
begin
{редко используем}
{операторы}
end.

